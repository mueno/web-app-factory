---
phase: 17-supabase-provisioning
plan: 03
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - web_app_factory/_supabase_provisioner.py
  - web_app_factory/_supabase_migration.py
  - tools/gates/supabase_gate.py
  - tests/test_supabase_provisioner.py
  - tests/test_supabase_migration.py
  - tests/test_supabase_gate.py
autonomous: true
requirements: [SUPA-01, SUPA-02, SUPA-03, SECG-02]

must_haves:
  truths:
    - "SupabaseProvisioner creates a project via Management API and polls until all services report ACTIVE_HEALTHY"
    - "Generated migration SQL includes ENABLE ROW LEVEL SECURITY and 4 CRUD policies (SELECT/INSERT/UPDATE/DELETE) per table"
    - "supabase_gate.py fails when any CREATE TABLE lacks a corresponding ENABLE ROW LEVEL SECURITY"
    - "supabase_gate.py verifies project existence via Management API health endpoint and reports failure if project is not ACTIVE_HEALTHY"
    - "supabase_gate.py verifies Vercel env vars (NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY) are injected by checking Vercel API"
    - "Provisioner stores db_pass in banto immediately after project creation (before any other operation)"
    - "Poll timeout prevents infinite hanging (300s max)"
    - "Empty health response list treated as not-yet-healthy (not all-healthy)"
  artifacts:
    - path: "web_app_factory/_supabase_provisioner.py"
      provides: "SupabaseProvisioner class with create_project, poll_until_healthy, get_api_keys, inject_vercel_env"
      exports: ["SupabaseProvisioner"]
    - path: "web_app_factory/_supabase_migration.py"
      provides: "Migration SQL generator with RLS policies"
      exports: ["generate_migration_sql"]
    - path: "tools/gates/supabase_gate.py"
      provides: "Supabase provisioning gate: RLS coverage scanner + project health check + Vercel env verification"
      exports: ["run_supabase_gate"]
    - path: "tests/test_supabase_provisioner.py"
      provides: "Provisioner unit tests with mocked httpx"
      contains: "ACTIVE_HEALTHY"
    - path: "tests/test_supabase_migration.py"
      provides: "Migration SQL generation tests"
      contains: "ENABLE ROW LEVEL SECURITY"
    - path: "tests/test_supabase_gate.py"
      provides: "Gate tests for RLS coverage, project health, and Vercel env injection"
      contains: "run_supabase_gate"
  key_links:
    - from: "web_app_factory/_supabase_provisioner.py"
      to: "web_app_factory._keychain.get_credential"
      via: "Retrieves supabase_access_token and supabase_org_id"
      pattern: "get_credential.*supabase"
    - from: "web_app_factory/_supabase_provisioner.py"
      to: "https://api.supabase.com/v1/projects"
      via: "httpx POST to create project, GET to poll health"
      pattern: "api\\.supabase\\.com"
    - from: "web_app_factory/_supabase_migration.py"
      to: "Generated SQL"
      via: "Constructs CREATE TABLE + ALTER TABLE ENABLE RLS + CREATE POLICY for each entity"
      pattern: "ENABLE ROW LEVEL SECURITY"
    - from: "tools/gates/supabase_gate.py"
      to: "web_app_factory._supabase_migration.py"
      via: "Regex scanning migration SQL for RLS coverage"
      pattern: "_check_rls_coverage"
    - from: "tools/gates/supabase_gate.py"
      to: "https://api.supabase.com/v1/projects/{ref}/health"
      via: "Management API health check for project existence verification"
      pattern: "api\\.supabase\\.com.*health"
    - from: "tools/gates/supabase_gate.py"
      to: "https://api.vercel.com/v10/projects/{id}/env"
      via: "Vercel API env var listing to verify credential injection"
      pattern: "api\\.vercel\\.com.*env"
---

<objective>
Build the SupabaseProvisioner, migration SQL generator, and supabase_gate.py for automated Supabase project provisioning with RLS-default security.

Purpose: This is the core of Phase 17 — when `waf_generate_app` runs with a Supabase-enabled spec, it must automatically create a Supabase project, apply a secure migration (RLS on every table), retrieve API keys, and inject them into Vercel. The supabase_gate verifies all of this happened correctly: RLS coverage, project health, and Vercel env injection.

Output: Three new production modules and three test files covering SUPA-01 (provisioner), SUPA-02 (migration SQL), SUPA-03 (gate verification with project health + Vercel env + RLS), and SECG-02 (RLS scanner).
</objective>

<execution_context>
@/Users/masa/.config/claude/get-shit-done/workflows/execute-plan.md
@/Users/masa/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-supabase-provisioning/17-CONTEXT.md
@.planning/phases/17-supabase-provisioning/17-RESEARCH.md
@.planning/phases/17-supabase-provisioning/17-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From web_app_factory/_keychain.py (modified by Plan 01):
```python
_BANTO_PROVIDER_MAP: dict[str, str] = {
    "supabase_access_token": "supabase-access-token",
    "supabase_org_id": "supabase-org-id",
    # ... existing keys
}

def get_credential(key: str) -> Optional[str]:
    """3-tier lookup: banto -> keyring -> env var."""
    ...

def store_credential(key: str, value: str) -> bool:
    """Store in banto (if available), else keyring."""
    ...
```

From tools/gates/gate_result.py:
```python
@dataclass(frozen=True)
class GateResult:
    gate_type: str = ""
    phase_id: str = ""
    passed: bool = False
    status: str = "BLOCKED"
    severity: str = "BLOCK"
    confidence: float = 0.0
    checked_at: str = ""
    issues: list = field(default_factory=list)
    advisories: list = field(default_factory=list)
    # ... (other fields)
```

From tools/gates/static_analysis_gate.py (pattern reference):
```python
def _should_skip(path: Path) -> bool:
    """Return True if the path should be skipped during scanning."""
    return "node_modules" in path.parts

def run_static_analysis_gate(project_dir: str, phase_id: str = "2b") -> GateResult:
    # Collects issues, returns GateResult with passed=True when no issues
```

Supabase Management API endpoints (from research):
```
POST https://api.supabase.com/v1/projects          -> Create project
GET  https://api.supabase.com/v1/projects/{ref}/health   -> Health status
GET  https://api.supabase.com/v1/projects/{ref}/api-keys  -> API keys
POST https://api.vercel.com/v10/projects/{id}/env   -> Inject env vars
GET  https://api.vercel.com/v10/projects/{id}/env   -> List env vars
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create SupabaseProvisioner and migration SQL generator</name>
  <files>web_app_factory/_supabase_provisioner.py, web_app_factory/_supabase_migration.py, tests/test_supabase_provisioner.py, tests/test_supabase_migration.py</files>
  <behavior>
    - Test: create_project() sends POST to /v1/projects with correct payload (name, org_slug, db_pass, region)
    - Test: create_project() generates db_pass using secrets.token_urlsafe(32) (cryptographically strong)
    - Test: create_project() returns dict with "ref" and "_db_pass" keys
    - Test: poll_until_healthy() polls GET /v1/projects/{ref}/health and returns when all services ACTIVE_HEALTHY
    - Test: poll_until_healthy() raises TimeoutError after timeout_s seconds
    - Test: poll_until_healthy() treats empty health list [] as not-yet-healthy
    - Test: poll_until_healthy() treats partial healthy (some services not ACTIVE_HEALTHY) as not-yet-healthy
    - Test: get_api_keys() extracts anon_key and service_role_key from API response
    - Test: inject_vercel_env() sends correct env vars with correct targets and types (NEXT_PUBLIC_ for anon, sensitive for service_role)
    - Test: generate_migration_sql() produces ENABLE ROW LEVEL SECURITY for every table
    - Test: generate_migration_sql() produces 4 CRUD policies (SELECT/INSERT/UPDATE/DELETE) per table
    - Test: generate_migration_sql() includes user_id uuid REFERENCES auth.users on every table
    - Test: generate_migration_sql() includes CREATE INDEX on user_id for every table
    - Test: generate_migration_sql() wraps auth.uid() in SELECT subquery: (SELECT auth.uid())
    - Test: generate_migration_sql() for 2 tables produces RLS + policies for BOTH tables
  </behavior>
  <action>
1. Create `web_app_factory/_supabase_provisioner.py` (~200 lines):
   - `SupabaseProvisioner` class with `__init__(self, access_token: str, org_id: str)`
   - Credential values NEVER logged (per security contract). Only operation names and status.
   - `_headers()` returns auth headers
   - `async create_project(self, name: str) -> dict`:
     - Generate db_pass with `secrets.token_urlsafe(32)`
     - POST to `https://api.supabase.com/v1/projects` with name, organization_slug, db_pass, region selection (americas smartGroup)
     - Return response JSON with `_db_pass` added (ephemeral, never serialized)
   - `async poll_until_healthy(self, ref: str, *, timeout_s: int = 300, interval_s: int = 5) -> None`:
     - GET `/v1/projects/{ref}/health` in a loop
     - All services must have `status == "ACTIVE_HEALTHY"`
     - Empty list = not yet healthy (Pitfall 5 from research)
     - Raise `TimeoutError` if deadline exceeded
   - `async get_api_keys(self, ref: str) -> dict[str, str]`:
     - GET `/v1/projects/{ref}/api-keys`
     - Return `{"anon_key": ..., "service_role_key": ...}`
   - `async inject_vercel_env(self, vercel_token: str, vercel_project_id: str, supabase_url: str, anon_key: str, service_role_key: str) -> None`:
     - POST to `https://api.vercel.com/v10/projects/{id}/env?upsert=true`
     - Set NEXT_PUBLIC_SUPABASE_URL (plain, all targets)
     - Set NEXT_PUBLIC_SUPABASE_ANON_KEY (plain, all targets)
     - Set SUPABASE_SERVICE_ROLE_KEY (sensitive, production+preview only -- NO NEXT_PUBLIC_ prefix)
   - Uses `httpx` (already in deps) for all HTTP calls

2. Create `web_app_factory/_supabase_migration.py` (~120 lines):
   - `generate_migration_sql(entities: list[dict]) -> str`:
     - `entities` is a list of dicts with `name` (table name) and `columns` (list of column dicts)
     - Every table gets `user_id uuid REFERENCES auth.users NOT NULL`
     - Every table gets `ENABLE ROW LEVEL SECURITY`
     - Every table gets 4 CRUD policies using `(SELECT auth.uid()) = user_id` pattern (with SELECT subquery wrapper per research anti-pattern note)
     - Every table gets `CREATE INDEX ON public.{table} (user_id)` (Pitfall 4)
     - Policies use `TO authenticated` role (not `TO public`)
   - Helper: `_generate_table_sql(entity: dict) -> str` for per-table SQL generation
   - Helper: `_generate_rls_policies(table_name: str) -> str` for the 4 CRUD policies

3. Create `tests/test_supabase_provisioner.py`:
   - Mock `httpx.AsyncClient` for all Management API calls
   - Test create_project: correct endpoint, payload structure, db_pass generation
   - Test poll_until_healthy: ACTIVE_HEALTHY exit, timeout, empty list handling, partial health
   - Test get_api_keys: correct parsing of anon/service_role
   - Test inject_vercel_env: correct env var targets and types

4. Create `tests/test_supabase_migration.py`:
   - Test single table: has CREATE TABLE, ENABLE RLS, 4 policies, index
   - Test two tables: both have RLS coverage
   - Test SQL uses (SELECT auth.uid()) pattern
   - Test every table has user_id REFERENCES auth.users

TDD: Write all test files first (RED), then implement production code (GREEN).
  </action>
  <verify>
    <automated>cd /Users/masa/Development/web-app-factory && python -m pytest tests/test_supabase_provisioner.py tests/test_supabase_migration.py -x -q</automated>
  </verify>
  <done>
    - _supabase_provisioner.py has SupabaseProvisioner with create_project, poll_until_healthy, get_api_keys, inject_vercel_env
    - _supabase_migration.py has generate_migration_sql producing RLS-enforced SQL for every table
    - All provisioner tests pass with mocked httpx
    - All migration tests pass with correct RLS patterns
    - No credential values in any log output
    - db_pass generated with secrets.token_urlsafe(32)
    - Empty health list handled as not-yet-healthy
    - Timeout raises TimeoutError
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create supabase_gate.py with RLS scanner, project health check, and Vercel env verification</name>
  <files>tools/gates/supabase_gate.py, tests/test_supabase_gate.py</files>
  <behavior>
    - Test: Gate returns passed=False when a CREATE TABLE has no corresponding ENABLE ROW LEVEL SECURITY
    - Test: Gate returns passed=True when all CREATE TABLEs have ENABLE ROW LEVEL SECURITY
    - Test: Gate detects table name in CREATE TABLE IF NOT EXISTS variant
    - Test: Gate handles CREATE TABLE with public. schema prefix
    - Test: Gate handles case-insensitive matching (create table, CREATE TABLE, Create Table)
    - Test: Gate reports specific table names that are missing RLS
    - Test: Gate returns GateResult with gate_type="supabase" and correct passed/status/severity fields
    - Test: Gate issues list contains descriptive messages for each missing-RLS table
    - Test: _check_project_health() returns issue when Management API health endpoint returns non-ACTIVE_HEALTHY
    - Test: _check_project_health() returns no issue when all services are ACTIVE_HEALTHY
    - Test: _check_project_health() returns issue when project ref is None or empty (no project provisioned)
    - Test: _check_vercel_env() returns issue when required env vars (NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY) are missing from Vercel project
    - Test: _check_vercel_env() returns no issue when all 3 required env vars are present
    - Test: _check_vercel_env() returns issue when vercel_token or vercel_project_id is None (not configured)
  </behavior>
  <action>
1. Create `tools/gates/supabase_gate.py` (~200 lines):
   - Import `GateResult` from `tools.gates.gate_result`
   - `_CREATE_TABLE_RE`: Regex matching `CREATE TABLE [IF NOT EXISTS] [public.]tablename`
   - `_ENABLE_RLS_RE`: Regex matching `ALTER TABLE [public.]tablename ENABLE ROW LEVEL SECURITY`

   - `_check_rls_coverage(migration_sql: str) -> list[str]`:
     - Extract all table names from CREATE TABLE statements
     - Extract all table names from ENABLE ROW LEVEL SECURITY statements
     - Return sorted list of tables in created but NOT in rls (missing RLS)

   - `_check_project_health(supabase_access_token: str | None, project_ref: str | None) -> list[str]`:
     - If project_ref is None or empty, return issue: "No Supabase project ref found -- project not provisioned"
     - If supabase_access_token is None, return issue: "SUPABASE_ACCESS_TOKEN not available -- cannot verify project health"
     - GET `https://api.supabase.com/v1/projects/{ref}/health` with auth header
     - If any service is not ACTIVE_HEALTHY or response is non-200, return issue describing the state
     - On success (all ACTIVE_HEALTHY), return empty list
     - Use `httpx` with timeout=15s, catch exceptions and return as advisory (don't block gate on transient network errors)

   - `_check_vercel_env(vercel_token: str | None, vercel_project_id: str | None) -> list[str]`:
     - If vercel_token or vercel_project_id is None, return issue: "Vercel credentials not available -- cannot verify env injection"
     - GET `https://api.vercel.com/v10/projects/{id}/env` to list env vars
     - Check that NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, and SUPABASE_SERVICE_ROLE_KEY are present
     - Return issue per missing env var
     - Catch exceptions and return as advisory

   - `run_supabase_gate(project_dir: str, phase_id: str = "3", *, supabase_access_token: str | None = None, project_ref: str | None = None, vercel_token: str | None = None, vercel_project_id: str | None = None) -> GateResult`:
     - Scan all `.sql` files under the project directory for migration SQL
     - Use `_check_rls_coverage` on each file -- these are BLOCKING issues
     - Use `_check_project_health` -- BLOCKING if project not found, ADVISORY if network error
     - Use `_check_vercel_env` -- BLOCKING if env vars missing, ADVISORY if credentials unavailable
     - Collect issues and advisories separately
     - Return GateResult with passed=True only if blocking issues list is empty
     - gate_type="supabase", severity="BLOCK" when failed

2. Create `tests/test_supabase_gate.py`:
   - Use `tmp_path` fixture to create temporary project dirs with .sql files
   - RLS tests:
     - Test: 2 tables both with RLS -> no RLS issues
     - Test: 2 tables, 1 missing RLS -> passed=False, issues mention specific table
     - Test: CREATE TABLE IF NOT EXISTS pattern detected correctly
     - Test: public. prefix handled correctly
     - Test: Case-insensitive matching
     - Test: Empty migration file -> passed=True (no tables to check)
   - Project health tests (mock httpx):
     - Test: All ACTIVE_HEALTHY -> no health issues
     - Test: Non-ACTIVE_HEALTHY -> issue returned
     - Test: project_ref is None -> issue returned
   - Vercel env tests (mock httpx):
     - Test: All 3 env vars present -> no env issues
     - Test: Missing SUPABASE_SERVICE_ROLE_KEY -> issue returned
     - Test: vercel_token is None -> advisory returned
   - Integration test: run_supabase_gate with all checks combined
   - GateResult shape verification (gate_type, passed, status, severity)

TDD: Write failing tests first (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>cd /Users/masa/Development/web-app-factory && python -m pytest tests/test_supabase_gate.py -x -q</automated>
  </verify>
  <done>
    - supabase_gate.py implements _check_rls_coverage, _check_project_health, and _check_vercel_env
    - run_supabase_gate combines all three checks into a single GateResult
    - Gate blocks when any table lacks ENABLE ROW LEVEL SECURITY
    - Gate blocks when Supabase project is not ACTIVE_HEALTHY
    - Gate blocks when required Vercel env vars are missing
    - Gate returns advisories (not blocking) when credentials are unavailable for health/env checks
    - GateResult follows established pattern (gate_type="supabase")
    - Regex handles CREATE TABLE variants (IF NOT EXISTS, public. prefix, case-insensitive)
    - All tests pass
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/masa/Development/web-app-factory && python -m pytest tests/test_supabase_provisioner.py tests/test_supabase_migration.py tests/test_supabase_gate.py -x -q
```
All provisioner, migration, and gate tests pass. Gate verifies RLS coverage, project health, and Vercel env injection.

Full phase verification:
```bash
cd /Users/masa/Development/web-app-factory && python -m pytest tests/ -x -q
```
Entire test suite green including all new and existing tests.
</verification>

<success_criteria>
1. SupabaseProvisioner creates project + polls until ACTIVE_HEALTHY (SUPA-01)
2. Migration SQL has ENABLE RLS + 4 policies per table + user_id index (SUPA-02)
3. supabase_gate.py verifies RLS on all tables + project health via Management API + Vercel env injection (SUPA-03, SECG-02)
4. db_pass generated with secrets.token_urlsafe(32)
5. Empty health list handled correctly (not treated as all-healthy)
6. TimeoutError raised on poll timeout
7. All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/17-supabase-provisioning/17-03-SUMMARY.md`
</output>
