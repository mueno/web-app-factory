---
phase: 09-deploy-abstraction
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tools/deploy_providers/__init__.py
  - tools/deploy_providers/base.py
  - tools/deploy_providers/registry.py
  - tools/deploy_providers/aws_provider.py
  - tools/deploy_providers/local_provider.py
  - tests/test_deploy_providers.py
autonomous: true
requirements:
  - DEPL-01
  - DEPL-04
  - DEPL-05

must_haves:
  truths:
    - "DeployProvider ABC cannot be instantiated directly"
    - "DeployResult is a frozen dataclass with success, url, provider, metadata fields"
    - "get_provider('aws') returns AWSProvider that raises NotImplementedError with v3.0 guidance"
    - "get_provider('local') returns LocalOnlyProvider that runs npm run build and returns localhost:3000"
    - "get_provider('unknown') raises ValueError listing valid targets"
    - "LocalOnlyProvider.verify() returns True without HTTP call"
  artifacts:
    - path: "tools/deploy_providers/__init__.py"
      provides: "Re-export hub: DeployProvider, DeployResult, get_provider"
      exports: ["DeployProvider", "DeployResult", "get_provider"]
    - path: "tools/deploy_providers/base.py"
      provides: "ABC with deploy/get_url/verify + frozen DeployResult dataclass"
      contains: "class DeployProvider"
    - path: "tools/deploy_providers/registry.py"
      provides: "VALID_DEPLOY_TARGETS, _PROVIDERS dict, get_provider() factory"
      exports: ["get_provider", "VALID_DEPLOY_TARGETS"]
    - path: "tools/deploy_providers/aws_provider.py"
      provides: "AWSProvider stub raising NotImplementedError"
      contains: "class AWSProvider"
    - path: "tools/deploy_providers/local_provider.py"
      provides: "LocalOnlyProvider running npm build and returning localhost URL"
      contains: "class LocalOnlyProvider"
    - path: "tests/test_deploy_providers.py"
      provides: "Unit tests for ABC, DeployResult, registry, AWSProvider, LocalOnlyProvider"
      min_lines: 80
  key_links:
    - from: "tools/deploy_providers/registry.py"
      to: "tools/deploy_providers/base.py"
      via: "imports DeployProvider type for registry dict"
      pattern: "from .base import DeployProvider"
    - from: "tools/deploy_providers/__init__.py"
      to: "tools/deploy_providers/registry.py"
      via: "re-exports get_provider"
      pattern: "from .registry import get_provider"
---

<objective>
Create the DeployProvider abstraction layer: ABC, DeployResult dataclass, provider registry, and two simple concrete providers (AWSProvider stub + LocalOnlyProvider).

Purpose: Establishes the provider contract that all deployment targets must satisfy. VercelProvider and GCPProvider (Plan 02 and 03) implement against this contract.
Output: `tools/deploy_providers/` module with base.py, registry.py, aws_provider.py, local_provider.py, __init__.py, plus full test coverage.
</objective>

<execution_context>
@/Users/masa/.config/claude/get-shit-done/workflows/execute-plan.md
@/Users/masa/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-deploy-abstraction/09-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From tools/gates/gate_result.py (frozen dataclass pattern to follow):
```python
@dataclass(frozen=True)
class GateResult:
    gate_type: str = ""
    phase_id: str = ""
    passed: bool = False
    # ... (frozen=True is the key convention)
```

From tools/phase_executors/base.py (ABC pattern already in use):
```python
class PhaseExecutor(ABC):
    @property
    @abstractmethod
    def phase_id(self) -> str: ...

    @abstractmethod
    def execute(self, ctx: PhaseContext) -> PhaseResult: ...
```

From tools/gates/deployment_gate.py (reused by verify()):
```python
def run_deployment_gate(url: str, phase_id: str = "3") -> GateResult:
    """HTTP GET on URL, expect 200. Provider-agnostic."""
```
</interfaces>
</context>

<feature>
  <name>DeployProvider ABC + Registry + AWSProvider + LocalOnlyProvider</name>
  <files>
    tools/deploy_providers/base.py,
    tools/deploy_providers/registry.py,
    tools/deploy_providers/aws_provider.py,
    tools/deploy_providers/local_provider.py,
    tools/deploy_providers/__init__.py,
    tests/test_deploy_providers.py
  </files>
  <behavior>
    RED phase tests (write tests first, all must fail):
    - test_abc_cannot_instantiate: DeployProvider() raises TypeError
    - test_deploy_result_fields: DeployResult(success=True, url="https://x.com", provider="test") is frozen, has .metadata
    - test_deploy_result_frozen: assignment to DeployResult.success raises FrozenInstanceError
    - test_registry_vercel: get_provider("vercel") returns instance (will fail until Plan 02 creates VercelProvider) -- SKIP this test in RED phase, mark xfail
    - test_registry_gcp: get_provider("gcp") returns instance (will fail until Plan 03 creates GCPProvider) -- SKIP this test in RED phase, mark xfail
    - test_registry_aws: get_provider("aws") returns AWSProvider instance
    - test_registry_local: get_provider("local") returns LocalOnlyProvider instance
    - test_registry_unknown: get_provider("banana") raises ValueError with valid targets in message
    - test_aws_deploy_raises: AWSProvider().deploy(...) raises NotImplementedError containing "v3.0"
    - test_aws_get_url_raises: AWSProvider().get_url(...) raises NotImplementedError
    - test_aws_verify_raises: AWSProvider().verify("http://x") raises NotImplementedError
    - test_local_deploy_success: LocalOnlyProvider().deploy(project_dir, env) calls subprocess with ["npm", "run", "build"], returns DeployResult(success=True, url="http://localhost:3000", provider="local")
    - test_local_deploy_missing_package_json: returns DeployResult(success=False) when package.json not in nextjs_dir
    - test_local_deploy_build_failure: returns DeployResult(success=False) when npm run build exits non-zero
    - test_local_verify_skips_http: LocalOnlyProvider().verify("http://localhost:3000") returns True without making HTTP calls
    - test_local_get_url: LocalOnlyProvider().get_url(DeployResult(success=True, url="http://localhost:3000", provider="local")) returns "http://localhost:3000"
    - test_local_get_url_no_url: LocalOnlyProvider().get_url(DeployResult(success=False, url=None, provider="local")) raises ValueError

    GREEN phase implementation:
    - base.py: DeployProvider ABC with 3 abstractmethods (deploy, get_url, verify) + frozen DeployResult dataclass
    - registry.py: _PROVIDERS dict mapping string to class, get_provider() factory, VALID_DEPLOY_TARGETS frozenset
    - aws_provider.py: AWSProvider with all 3 methods raising NotImplementedError with v3.0 guidance message
    - local_provider.py: LocalOnlyProvider that runs npm run build via subprocess (no shell=True), returns http://localhost:3000 on success, checks package.json existence, verify() returns True unconditionally
    - __init__.py: re-export DeployProvider, DeployResult, get_provider
  </behavior>
  <implementation>
    Follow the research patterns exactly:
    - DeployResult mirrors GateResult convention (frozen=True)
    - DeployProvider mirrors PhaseExecutor ABC convention
    - AWSProvider._GUIDANCE message: "AWS deployment is not implemented in web-app-factory v2.0. It is planned for v3.0. To deploy manually, use the AWS CDK: https://docs.aws.amazon.com/cdk/v2/guide/getting_started.html"
    - LocalOnlyProvider.deploy(): check package.json exists, run subprocess.run(["npm", "run", "build"], cwd=nextjs_dir, ...), return DeployResult with http://localhost:3000 on success
    - LocalOnlyProvider reads nextjs_dir from env.get("nextjs_dir") or str(project_dir)
    - LocalOnlyProvider.verify() returns True without HTTP check (per user decision: deployment_gate skipped for local-only)
    - Registry: mark vercel and gcp entries with the classes once Plans 02/03 create them; for now import them in registry.py (they will exist by the time the full phase is assembled). Use a lazy import pattern or forward reference if needed.
    - IMPORTANT: All subprocess calls must use list args (no shell=True) -- the subprocess audit test scans tools/ recursively
    - npm run build timeout: 300 seconds
    - env={**os.environ} on all subprocess calls

    NOTE on registry imports: Since VercelProvider and GCPProvider will be created by Plans 02 and 03 respectively, the registry.py should import all four providers. However, Plans 02/03 run in Wave 2 after this plan. To handle this: create placeholder files for vercel_provider.py and gcp_provider.py that define minimal classes inheriting DeployProvider and raising NotImplementedError (similar to AWSProvider but with message "Provider not yet implemented -- see Plan 09-02/09-03"). Plans 02/03 will overwrite these files with full implementations. This ensures registry.py imports work immediately and tests can mark vercel/gcp registry tests as xfail until Plans 02/03 complete.

    ALTERNATIVE (simpler): Just create the placeholder files as empty stubs. The registry tests for vercel/gcp can be pytest.mark.xfail.
  </implementation>
</feature>

<verification>
<automated>cd /Users/masa/Development/web-app-factory && python3 -m pytest tests/test_deploy_providers.py -x -q && python3 -m pytest tests/test_subprocess_audit.py -x -q</automated>
</verification>

<success_criteria>
- tools/deploy_providers/ module exists with 6 files (base.py, registry.py, aws_provider.py, local_provider.py, __init__.py, and placeholder stubs for vercel/gcp)
- DeployProvider ABC enforces deploy/get_url/verify contract
- DeployResult is a frozen dataclass with success, url, provider, metadata fields
- AWSProvider raises NotImplementedError with v3.0 guidance on all methods
- LocalOnlyProvider runs npm run build and returns localhost:3000 on success
- LocalOnlyProvider.verify() returns True without HTTP
- get_provider() factory returns correct instances; ValueError on unknown target
- Subprocess audit test still passes (no shell=True in new files)
- All non-xfail tests in test_deploy_providers.py pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-deploy-abstraction/09-01-SUMMARY.md`
</output>
