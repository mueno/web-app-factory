---
phase: 01-infrastructure
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - tools/factory_mcp_server.py
  - config/settings.py
  - agents/definitions.py
  - pipeline_runtime/error_router.py
  - tools/phase_executors/phase_stubs.py
  - tools/quality_self_assessment.py
  - tests/test_factory_mcp_bridge.py
  - tests/test_quality_assessment.py
autonomous: true
requirements:
  - PIPE-04
  - CONT-04

must_haves:
  truths:
    - "MCP server starts and provides approve_gate and phase_reporter tools"
    - "phase_reporter MCP tool updates state.json via the project_dir bridge (not just local logging)"
    - "Quality self-assessment JSON is generated before gate submission"
    - "Phase executor stubs exist for all 5 phases so the pipeline runner can load without crashing"
  artifacts:
    - path: "tools/factory_mcp_server.py"
      provides: "FastMCP server with approve_gate + phase_reporter tools"
      exports: ["mcp"]
    - path: "config/settings.py"
      provides: "Project-level settings (approval dir, paths)"
      exports: ["APPROVAL_TMP_DIR"]
    - path: "agents/definitions.py"
      provides: "Stub agent definitions for web pipeline"
    - path: "pipeline_runtime/error_router.py"
      provides: "Failure classification with web error patterns"
    - path: "tools/phase_executors/phase_stubs.py"
      provides: "Stub executors for all 5 phases"
    - path: "tools/quality_self_assessment.py"
      provides: "Quality self-assessment generator for CONT-04"
      exports: ["generate_quality_self_assessment"]
    - path: "tests/test_factory_mcp_bridge.py"
      provides: "CRITICAL integration test: phase_reporter -> state.json"
    - path: "tests/test_quality_assessment.py"
      provides: "Quality self-assessment generation test"
  key_links:
    - from: "tools/factory_mcp_server.py"
      to: "tools/pipeline_state.py"
      via: "phase_reporter calls pipeline_state functions via project_dir bridge"
      pattern: "pipeline_state\\.(phase_start|phase_complete)"
    - from: "tools/quality_self_assessment.py"
      to: "contracts/pipeline-contract.web.v1.yaml"
      via: "Reads quality_criteria from contract deliverables"
      pattern: "quality_criteria"
---

<objective>
Set up MCP server, config, agent stubs, error router, phase executor stubs, and quality self-assessment.

Purpose: The MCP server (PIPE-04) provides the human-in-the-loop approval gate. The phase_reporter MCP tool's bridge to state.json is the single most dangerous integration point (see MEMORY.md: Dual Implementation Divergence). Quality self-assessment (CONT-04) ensures gate-gaming prevention is built in from day one.

Output: MCP server adapted from ios-app-factory (iOS tools removed), config module, agent stubs, error router with web patterns, phase executor stubs for all 5 phases, quality self-assessment module, and the CRITICAL integration test for MCP-to-state bridge.
</objective>

<execution_context>
@/Users/masa/.config/claude/get-shit-done/workflows/execute-plan.md
@/Users/masa/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-infrastructure/01-CONTEXT.md
@.planning/phases/01-infrastructure/01-RESEARCH.md

<interfaces>
<!-- Source files to copy/adapt from ios-app-factory -->

From /Users/masa/Development/ios-app-factory/tools/factory_mcp_server.py:
- approve_gate tool (file-polling approval flow) — keep verbatim
- phase_reporter tool with project_dir bridge — keep verbatim (CRITICAL)
- Remove: render_legal_template, render_pera1_pitch_html, and their imports
- Update: from config.settings import APPROVAL_TMP_DIR

From /Users/masa/Development/ios-app-factory/pipeline_runtime/error_router.py:
- ErrorRouter class with _FAILURE_PATTERNS
- Replace iOS patterns (xcodebuild, xcresult) with web patterns (npm, next build, tsc)
- Keep router structure verbatim

From /Users/masa/Development/ios-app-factory/config/settings.py:
- APPROVAL_TMP_DIR and other path constants
- Remove iOS-specific paths (ASC, Xcode, etc.)
- Add web-specific paths (VERCEL_CONFIG_DIR, etc.)

<!-- Dependencies from Plan 01 and Plan 02 -->

From pyproject.toml (Plan 01):
- fastmcp, mcp packages available

From tools/pipeline_state.py (Plan 02):
- init_run, load_state, phase_start, phase_complete, get_resume_phase
- PHASE_ORDER = ["1a", "1b", "2a", "2b", "3"]

From tools/phase_executors/base.py (Plan 02):
- PhaseExecutor ABC, PhaseContext, PhaseResult

From contracts/pipeline-contract.web.v1.yaml (Plan 01):
- quality_criteria arrays per deliverable
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adapt MCP server, config, error router, agent stubs, phase stubs</name>
  <files>
    tools/factory_mcp_server.py,
    config/settings.py,
    agents/definitions.py,
    pipeline_runtime/error_router.py,
    tools/phase_executors/phase_stubs.py
  </files>
  <action>
    1. **config/settings.py**: Read `/Users/masa/Development/ios-app-factory/config/settings.py`. Adapt by:
       - Keep APPROVAL_TMP_DIR (used by MCP server for file-based polling)
       - Remove all iOS/ASC paths (ASC_API_URL, XCODE_*, SIMULATOR_*, etc.)
       - Add web equivalents: VERCEL_CONFIG_DIR, DEFAULT_FRAMEWORK="nextjs", DEFAULT_DEPLOY_TARGET="vercel"
       - Keep PROJECT_ROOT pattern for finding the factory root directory

    2. **tools/factory_mcp_server.py**: Read `/Users/masa/Development/ios-app-factory/tools/factory_mcp_server.py`. Adapt by:
       - Keep `approve_gate` tool VERBATIM (file-based polling is domain-agnostic)
       - Keep `phase_reporter` tool VERBATIM — CRITICAL: preserve the `project_dir` bridge that calls `pipeline_state.phase_start()` and `pipeline_state.phase_complete()`. This bridge is what makes state.json update. Without it, the MCP server logs locally but state.json stays stale (HealthStockBoardV30 root cause).
       - REMOVE: `render_legal_template` tool and its import (`from tools.factory_mcp_renderers import ...`)
       - REMOVE: `render_pera1_pitch_html` tool and its import (`from tools.template_renderer import ...`)
       - REMOVE: Any other iOS-specific tool registrations
       - UPDATE: `from config.settings import APPROVAL_TMP_DIR` import (same path, just ensure config/settings.py provides it)
       - The FastMCP server object name should remain `mcp` for consistency

    3. **pipeline_runtime/error_router.py**: Read `/Users/masa/Development/ios-app-factory/pipeline_runtime/error_router.py`. Adapt by:
       - Keep the ErrorRouter class structure and routing logic VERBATIM
       - Replace iOS failure patterns: "xcodebuild" -> "next build", "xcresult" -> "build output", "BUILD FAILED" -> "Build failed", "signing" -> "deployment"
       - Replace agent names: "build-agent" (keep), "test-agent" -> "test-agent" (keep), "legal-agent" (keep), "ship-agent" -> "deploy-agent"
       - Keep confidence thresholds and escalation logic

    4. **agents/definitions.py**: Create NEW file (do NOT copy from ios-app-factory). Define stub agent definitions:
       - AgentDefinition dataclass (or simple dict) with name, description, system_prompt fields
       - Three stubs: spec-agent ("Validates ideas and generates PRDs"), build-agent ("Scaffolds and builds Next.js applications"), deploy-agent ("Deploys to Vercel and runs quality gates")
       - System prompts should be placeholder strings: "System prompt to be defined in Phase 2"
       - This file is a stub — full agent definitions are Phase 2+ work

    5. **tools/phase_executors/phase_stubs.py**: Create stub executors for all 5 phases following the pattern from RESEARCH.md Pattern 6:
       - Phase1aStubExecutor, Phase1bStubExecutor, Phase2aStubExecutor, Phase2bStubExecutor, Phase3StubExecutor
       - Each inherits from PhaseExecutor, returns PhaseResult(success=False, error="Phase {id} executor not yet implemented")
       - Register all stubs in the executor registry if registry.py supports auto-registration, or export a list for manual registration
  </action>
  <verify>
    <automated>cd /Users/masa/Development/web-app-factory && uv run python -c "
from config.settings import APPROVAL_TMP_DIR
print('APPROVAL_TMP_DIR:', APPROVAL_TMP_DIR)
from agents.definitions import AGENT_DEFINITIONS
print('Agents:', [a['name'] if isinstance(a, dict) else a.name for a in AGENT_DEFINITIONS])
from tools.phase_executors.phase_stubs import Phase1aStubExecutor
print('Stub executor phase_id:', Phase1aStubExecutor().phase_id)
print('all imports ok')
"</automated>
  </verify>
  <done>MCP server adapted (iOS tools removed, project_dir bridge preserved), config has web paths, 3 agent stubs defined, error router has web patterns, 5 phase executor stubs created</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create quality self-assessment module and critical MCP bridge test</name>
  <files>
    tools/quality_self_assessment.py,
    tests/test_factory_mcp_bridge.py,
    tests/test_quality_assessment.py
  </files>
  <behavior>
    - Test: generate_quality_self_assessment reads quality_criteria from contract YAML and produces a JSON file
    - Test: The generated JSON has the correct structure (phase_id, deliverables, each with criteria and status)
    - Test: The generated JSON file is saved to docs/pipeline/quality-self-assessment-{phase_id}.json
    - Test: phase_reporter MCP tool with project_dir parameter updates state.json (CRITICAL integration test)
    - Test: After phase_reporter reports "start" for phase "1a", state.json shows phase "1a" as "in_progress"
  </behavior>
  <action>
    1. **tools/quality_self_assessment.py**: Create a module that:
       - `generate_quality_self_assessment(phase_id: str, project_dir: str, contract_path: str) -> dict`
       - Reads the YAML contract to find the phase's deliverables and quality_criteria
       - Generates a JSON structure:
         ```json
         {
           "phase_id": "1a",
           "timestamp": "ISO-8601",
           "deliverables": [
             {
               "name": "Idea Validation Report",
               "path": "docs/idea-validation.md",
               "criteria": [
                 {"criterion": "3+ named competitor apps...", "status": "pending", "evidence": null}
               ]
             }
           ]
         }
         ```
       - Writes the JSON to `{project_dir}/docs/pipeline/quality-self-assessment-{phase_id}.json`
       - Returns the dict for programmatic use

    2. **tests/test_quality_assessment.py**: Test the quality self-assessment module:
       - Test with a real contract file: generate assessment for phase "1a", verify output has correct deliverables from the contract
       - Test file output: verify JSON file is created at expected path
       - Test structure: each deliverable has criteria list, each criterion has status "pending"

    3. **tests/test_factory_mcp_bridge.py**: CRITICAL integration test per CONTEXT.md locked decision and MEMORY.md Dual Implementation Divergence warning:
       - This test verifies that the MCP `phase_reporter` tool's `project_dir` bridge actually updates `state.json`
       - Setup: create a temp project dir, init a pipeline run via pipeline_state.init_run()
       - Action: Call the phase_reporter tool function directly (not via MCP transport — test the Python function, not the network layer) with action="start", phase_id="1a", project_dir=temp_dir
       - Assert: state.json in temp_dir shows phase "1a" with status "in_progress"
       - Action: Call phase_reporter with action="complete", phase_id="1a", project_dir=temp_dir
       - Assert: state.json shows phase "1a" with status "completed"
       - This test is the safety net against the HealthStockBoardV30 failure mode

       NOTE: The exact function signature of phase_reporter depends on how factory_mcp_server.py implements it. Read the copied file to determine the correct call pattern. The function may be decorated with `@mcp.tool()` — call the underlying function directly.
  </action>
  <verify>
    <automated>cd /Users/masa/Development/web-app-factory && uv run pytest tests/test_quality_assessment.py tests/test_factory_mcp_bridge.py -v</automated>
  </verify>
  <done>Quality self-assessment module generates correct JSON from contract quality_criteria. MCP bridge integration test proves phase_reporter updates state.json (the HealthStockBoardV30 safety net).</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_factory_mcp_bridge.py tests/test_quality_assessment.py -v` all green
- MCP server file has no iOS-specific tool imports or registrations
- phase_reporter preserves the project_dir bridge to pipeline_state
- Quality self-assessment reads quality_criteria from the YAML contract
- 5 phase executor stubs exist and are importable
</verification>

<success_criteria>
- MCP server provides approve_gate and phase_reporter tools (iOS tools removed)
- phase_reporter updates state.json via project_dir bridge (verified by integration test)
- Quality self-assessment generates JSON from contract quality_criteria before gate
- Config module provides APPROVAL_TMP_DIR and web-specific paths
- Agent stubs define spec-agent, build-agent, deploy-agent
- Error router has web failure patterns (not iOS)
- 5 phase executor stubs exist for pipeline runner loading
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure/01-03-SUMMARY.md`
</output>
