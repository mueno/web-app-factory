---
phase: 01-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - contracts/pipeline-contract.web.v1.yaml
  - contracts/pipeline-contract.schema.json
  - tools/__init__.py
  - tools/gates/__init__.py
  - tools/phase_executors/__init__.py
  - agents/__init__.py
  - pipeline_runtime/__init__.py
  - config/__init__.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_contract_schema.py
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - CONT-03

must_haves:
  truths:
    - "uv sync installs all dependencies without error"
    - "YAML contract defines 5 phases (1a, 1b, 2a, 2b, 3) with purpose, deliverables, quality_criteria, and gates"
    - "Every deliverable in the contract has at least one quality_criteria entry that verifies content (not just file existence)"
    - "JSON schema validation passes on the YAML contract at startup"
  artifacts:
    - path: "pyproject.toml"
      provides: "Python project configuration with all dependencies"
      contains: "web-app-factory"
    - path: "contracts/pipeline-contract.web.v1.yaml"
      provides: "5-phase pipeline contract with quality criteria"
      contains: "quality_criteria"
    - path: "contracts/pipeline-contract.schema.json"
      provides: "JSON Schema for contract validation (CONT-03)"
      contains: "pipeline-contract"
    - path: "tests/conftest.py"
      provides: "Shared test fixtures for all test files"
    - path: "tests/test_contract_schema.py"
      provides: "Contract parsing, schema validation, and quality criteria tests"
  key_links:
    - from: "contracts/pipeline-contract.schema.json"
      to: "contracts/pipeline-contract.web.v1.yaml"
      via: "jsonschema.validate() in test"
      pattern: "jsonschema\\.validate"
    - from: "contracts/pipeline-contract.web.v1.yaml"
      to: "tools/pipeline_state.py"
      via: "Phase IDs must match PHASE_ORDER"
      pattern: "1a.*1b.*2a.*2b.*3"
---

<objective>
Create the project skeleton and pipeline contract for web-app-factory.

Purpose: Everything else in Phase 1 builds on top of the project structure and contract definition. The YAML contract is the schema that pipeline_state, governance_monitor, and CLI all reference. Writing it first prevents downstream rework.

Output: A Python project with pyproject.toml, empty package directories, the 5-phase YAML contract, its JSON schema, shared test fixtures, and contract validation tests.
</objective>

<execution_context>
@/Users/masa/.config/claude/get-shit-done/workflows/execute-plan.md
@/Users/masa/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure/01-CONTEXT.md
@.planning/phases/01-infrastructure/01-RESEARCH.md
@.planning/phases/01-infrastructure/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create project skeleton and pyproject.toml</name>
  <files>
    pyproject.toml,
    tools/__init__.py,
    tools/gates/__init__.py,
    tools/phase_executors/__init__.py,
    agents/__init__.py,
    pipeline_runtime/__init__.py,
    config/__init__.py,
    tests/__init__.py,
    tests/conftest.py
  </files>
  <behavior>
    - Test: `uv sync` exits 0 (dependencies install)
    - Test: All required packages importable (pyyaml, jsonschema, fastmcp, httpx)
    - Test: conftest.py provides `tmp_project_dir` fixture that creates a temp directory with `docs/pipeline/` subdirectory
  </behavior>
  <action>
    Create pyproject.toml with the following configuration per CONTEXT.md locked decisions:
    - name: "web-app-factory", version: "0.1.0", requires-python: ">=3.10"
    - dependencies: claude-agent-sdk>=0.1.50, fastmcp>=3.1.0, httpx>=0.28.0, jsonschema>=4.20.0, mcp>=1.26.0, pyyaml>=6.0
    - dev dependencies: mypy>=1.19.1, pytest>=9.0.2, ruff>=0.15.4
    - [tool.pytest.ini_options]: testpaths = ["tests"]
    - [tool.ruff]: target-version = "py310", line-length = 120

    Create empty __init__.py files in: tools/, tools/gates/, tools/phase_executors/, agents/, pipeline_runtime/, config/, tests/

    Create tests/conftest.py with shared fixtures:
    - `tmp_project_dir(tmp_path)`: Creates a temporary project directory with `docs/pipeline/runs/` subdirectory, returns the Path. Used by pipeline_state and MCP bridge tests.
    - `sample_contract_path()`: Returns the path to `contracts/pipeline-contract.web.v1.yaml`

    Run `uv sync` to generate uv.lock and verify all dependencies install.
  </action>
  <verify>
    <automated>cd /Users/masa/Development/web-app-factory && uv sync && uv run python -c "import yaml; import jsonschema; import httpx; print('imports ok')"</automated>
  </verify>
  <done>pyproject.toml exists with all specified dependencies, uv.lock is generated, all package directories have __init__.py, conftest.py provides fixtures</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create YAML contract, JSON schema, and contract validation tests</name>
  <files>
    contracts/pipeline-contract.web.v1.yaml,
    contracts/pipeline-contract.schema.json,
    tests/test_contract_schema.py
  </files>
  <behavior>
    - Test: YAML contract file parses without error
    - Test: Contract has exactly 5 phases with IDs ["1a", "1b", "2a", "2b", "3"]
    - Test: Every phase has purpose, deliverables, and gates keys
    - Test: Every deliverable has name, path, and quality_criteria with >= 1 item
    - Test: No quality_criteria string is a pure file-existence check (must contain content-verifying language)
    - Test: JSON schema validation passes on the YAML contract
    - Test: JSON schema validation rejects a contract missing required fields
    - Test: JSON schema validation rejects a deliverable with empty quality_criteria
  </behavior>
  <action>
    Create `contracts/pipeline-contract.web.v1.yaml` following the structure from RESEARCH.md Pattern 4. Define all 5 phases:

    Phase 1a (Idea Validation): Purpose is market/technical validation. Deliverables: idea-validation.md (quality_criteria: 3+ named competitors with feature comparison, target user persona with concrete pain point, differentiation derived from analysis, 3+ risks with mitigation), tech-feasibility-memo.json (quality_criteria: Next.js suitability evaluated, external API dependencies listed, Vercel constraints noted). Gates: artifact (required_files) + tool_invocation (content markers).

    Phase 1b (Spec and Design): Purpose is structured PRD. Deliverables: prd.md (quality_criteria: MoSCoW classified requirements, component inventory with hierarchy, responsive breakpoint strategy, route structure), screen-spec.json (quality_criteria: every screen has layout description and component list). Gates: artifact + tool_invocation.

    Phase 2a (Scaffold): Purpose is create-next-app scaffold. Deliverables: Next.js project at src/ (quality_criteria: TypeScript strict mode, Tailwind v4 configured, App Router with layout.tsx). Gates: build (next build passes, tsc --noEmit passes).

    Phase 2b (Build): Purpose is generate pages/components from PRD. Deliverables: generated app (quality_criteria: all PRD screens implemented, error boundaries present, mobile-first responsive). Gates: build + static_analysis (no "use client" in layout.tsx/page.tsx, no NEXT_PUBLIC_ secret leak).

    Phase 3 (Ship): Purpose is quality gates + legal + deploy. Deliverables: deployed app (quality_criteria: Lighthouse perf>=85/a11y>=90/SEO>=85, axe-core zero critical, security headers present), legal docs (quality_criteria: Privacy Policy references actual features, ToS references actual features). Gates: lighthouse + accessibility + security_headers + link_integrity + deployment + mcp_approval.

    Include top-level fields: version "1", schema "pipeline-contract", metadata (created date, description), defaults (strict_mode: true, gate_policy: fail-closed, strict_quality_assessment_required: true).

    Create `contracts/pipeline-contract.schema.json` as a JSON Schema (draft-07) that validates the contract structure. Required top-level: version, schema, phases. Each phase requires: id, name, purpose, deliverables, gates. Each deliverable requires: name, path, quality_criteria (array, minItems: 1). Each gate requires: type, description.

    Create `tests/test_contract_schema.py` implementing the behavior tests above. Use `yaml.safe_load()` to parse the contract and `jsonschema.validate()` to validate against the schema. Include a test that verifies quality_criteria strings contain content-verifying language (reject strings that only check existence like "file exists" or "is present" without further content requirements).
  </action>
  <verify>
    <automated>cd /Users/masa/Development/web-app-factory && uv run pytest tests/test_contract_schema.py -v</automated>
  </verify>
  <done>YAML contract defines 5 phases with content-verifying quality criteria, JSON schema validates the contract structure, all 8+ contract tests pass</done>
</task>

</tasks>

<verification>
- `uv sync` completes without error
- `uv run pytest tests/test_contract_schema.py -v` shows all tests passing
- YAML contract has 5 phases with IDs matching the web pipeline
- Every quality_criteria entry is content-verifying, not existence-only
</verification>

<success_criteria>
- pyproject.toml exists with all dependencies, uv.lock generated
- All package directories have __init__.py
- YAML contract defines 5 phases (1a, 1b, 2a, 2b, 3) with quality criteria
- JSON schema validates the contract successfully
- Contract validation tests pass (parsing, schema, quality criteria content)
- conftest.py provides shared fixtures for downstream test files
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure/01-01-SUMMARY.md`
</output>
