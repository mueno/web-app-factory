---
phase: 01-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - tools/pipeline_state.py
  - pipeline_runtime/governance_monitor.py
  - tools/gates/gate_result.py
  - tools/gates/gate_policy.py
  - tools/phase_executors/base.py
  - tools/phase_executors/registry.py
  - tests/test_pipeline_state.py
  - tests/test_governance_monitor.py
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02
  - PIPE-03
  - PIPE-05

must_haves:
  truths:
    - "Pipeline state initializes a run, persists phases to state.json, and appends to activity-log.jsonl"
    - "Pipeline resumes from the correct phase after interruption (no re-run of completed phases)"
    - "Governance monitor raises GovernanceViolationError when phases are executed out of order"
    - "Governance monitor raises GovernanceViolationError on direct file edits to guarded files"
    - "PHASE_ORDER constant matches the 5 web phases: 1a, 1b, 2a, 2b, 3"
  artifacts:
    - path: "tools/pipeline_state.py"
      provides: "State persistence (state.json + activity-log.jsonl) and resume logic"
      contains: "PHASE_ORDER"
      exports: ["init_run", "load_state", "phase_start", "phase_complete", "get_resume_phase"]
    - path: "pipeline_runtime/governance_monitor.py"
      provides: "Phase ordering enforcement and bypass detection"
      exports: ["GovernanceViolationError", "GovernanceMonitor"]
    - path: "tools/gates/gate_result.py"
      provides: "GateResult frozen dataclass for gate pass/fail results"
      exports: ["GateResult"]
    - path: "tools/gates/gate_policy.py"
      provides: "Gate pass/fail policy logic"
    - path: "tools/phase_executors/base.py"
      provides: "PhaseExecutor ABC, PhaseContext, PhaseResult dataclasses"
      exports: ["PhaseExecutor", "PhaseContext", "PhaseResult"]
    - path: "tools/phase_executors/registry.py"
      provides: "Phase executor registry for dynamic dispatch"
  key_links:
    - from: "tools/pipeline_state.py"
      to: "contracts/pipeline-contract.web.v1.yaml"
      via: "PHASE_ORDER constant matches contract phase IDs"
      pattern: "PHASE_ORDER.*1a.*1b.*2a.*2b.*3"
    - from: "pipeline_runtime/governance_monitor.py"
      to: "tools/pipeline_state.py"
      via: "Reads state to enforce phase ordering"
      pattern: "phase_order_violation"
---

<objective>
Copy and adapt the pipeline backbone from ios-app-factory: state persistence, governance enforcement, gates, and phase executor infrastructure.

Purpose: These files form the runtime backbone that Plan 03 (MCP + config) and Plan 04 (CLI + preflight) depend on. State persistence enables PIPE-02/03 (state survives interruption, resume works). Governance monitor enables PIPE-05 (bypass detection). Phase executor base enables the stub pattern used in Plan 03.

Output: 6 production files (copied/adapted from ios-app-factory) and 2 test files verifying state and governance behavior.
</objective>

<execution_context>
@/Users/masa/.config/claude/get-shit-done/workflows/execute-plan.md
@/Users/masa/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-infrastructure/01-CONTEXT.md
@.planning/phases/01-infrastructure/01-RESEARCH.md

<interfaces>
<!-- Source files to copy from ios-app-factory. Read these during execution. -->

From /Users/masa/Development/ios-app-factory/tools/pipeline_state.py:
- PHASE_ORDER constant (update to ["1a", "1b", "2a", "2b", "3"])
- PHASE_LABELS dict (update to web phase names)
- Functions: init_run, load_state, phase_start, phase_complete, mark_failed, find_latest_run, get_resume_phase

From /Users/masa/Development/ios-app-factory/pipeline_runtime/governance_monitor.py:
- GovernanceViolationError class
- GovernanceMonitor class
- Violation kinds: phase_order_violation, write_without_phase_start, gate_before_phase_violation
- Copy verbatim — all logic is domain-agnostic

From /Users/masa/Development/ios-app-factory/tools/gates/gate_result.py:
- GateResult frozen dataclass
- Copy verbatim

From /Users/masa/Development/ios-app-factory/tools/gates/gate_policy.py:
- Gate pass/fail policy
- Copy verbatim

From /Users/masa/Development/ios-app-factory/tools/phase_executors/base.py:
- PhaseContext, PhaseResult, SubStepResult dataclasses
- PhaseExecutor ABC with phase_id, sub_steps, execute()
- Copy verbatim

From /Users/masa/Development/ios-app-factory/tools/phase_executors/registry.py:
- Executor registry for dynamic dispatch
- Copy verbatim (will be populated with web executors in Phase 2)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Copy/adapt pipeline state, governance, gates, and phase executor base</name>
  <files>
    tools/pipeline_state.py,
    pipeline_runtime/governance_monitor.py,
    tools/gates/gate_result.py,
    tools/gates/gate_policy.py,
    tools/phase_executors/base.py,
    tools/phase_executors/registry.py
  </files>
  <behavior>
    - Test: PHASE_ORDER equals ["1a", "1b", "2a", "2b", "3"]
    - Test: init_run creates state.json in the project dir
    - Test: phase_start + phase_complete updates state.json
    - Test: activity-log.jsonl is appended after phase lifecycle events
    - Test: get_resume_phase returns the first incomplete phase
    - Test: get_resume_phase returns None when all phases complete
    - Test: GovernanceViolationError raised for phase_order_violation
    - Test: GovernanceViolationError raised for write_without_phase_start
    - Test: GateResult.passed is False by default, True when explicitly set
  </behavior>
  <action>
    Read the following ios-app-factory source files and copy them into web-app-factory:

    1. `tools/pipeline_state.py` — Read from `/Users/masa/Development/ios-app-factory/tools/pipeline_state.py`. Copy the entire file. Then make exactly two changes:
       - Update `PHASE_ORDER` to `["1a", "1b", "2a", "2b", "3"]`
       - Update `PHASE_LABELS` to `{"1a": "Idea Validation", "1b": "Spec & Design", "2a": "Scaffold", "2b": "Build", "3": "Ship"}`
       - Do NOT change any function logic. Every function (init_run, load_state, phase_start, phase_complete, mark_failed, find_latest_run, get_resume_phase) must remain exactly as-is.

    2. `pipeline_runtime/governance_monitor.py` — Read from `/Users/masa/Development/ios-app-factory/pipeline_runtime/governance_monitor.py`. Copy VERBATIM. Zero changes. All violation kinds (phase_order_violation, write_without_phase_start, gate_before_phase_violation) are domain-agnostic.

    3. `tools/gates/gate_result.py` — Read from `/Users/masa/Development/ios-app-factory/tools/gates/gate_result.py`. Copy VERBATIM.

    4. `tools/gates/gate_policy.py` — Read from `/Users/masa/Development/ios-app-factory/tools/gates/gate_policy.py`. Copy VERBATIM.

    5. `tools/phase_executors/base.py` — Read from `/Users/masa/Development/ios-app-factory/tools/phase_executors/base.py`. Copy VERBATIM. Contains PhaseContext, PhaseResult, SubStepResult, PhaseExecutor ABC.

    6. `tools/phase_executors/registry.py` — Read from `/Users/masa/Development/ios-app-factory/tools/phase_executors/registry.py`. Copy VERBATIM. Empty registry that gets populated when executors register.

    CRITICAL: After copying, verify each file has no ios-app-factory-specific import paths that would fail (e.g., imports from `tools.asc_*` or `tools.reject_fix_*`). Fix any broken imports. The domain-agnostic files should not have such imports, but verify.
  </action>
  <verify>
    <automated>cd /Users/masa/Development/web-app-factory && uv run python -c "from tools.pipeline_state import PHASE_ORDER, init_run, get_resume_phase; from pipeline_runtime.governance_monitor import GovernanceViolationError; from tools.gates.gate_result import GateResult; from tools.phase_executors.base import PhaseExecutor, PhaseContext, PhaseResult; print('PHASE_ORDER:', PHASE_ORDER); assert PHASE_ORDER == ['1a', '1b', '2a', '2b', '3'], f'Wrong: {PHASE_ORDER}'; print('all imports ok')"</automated>
  </verify>
  <done>All 6 files exist, PHASE_ORDER is updated to web phases, all imports resolve without error, no iOS-specific references remain</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create tests for pipeline state and governance monitor</name>
  <files>
    tests/test_pipeline_state.py,
    tests/test_governance_monitor.py
  </files>
  <behavior>
    - Test: PHASE_ORDER has exactly 5 web phase IDs
    - Test: init_run creates state.json with correct structure (run_id, phases dict)
    - Test: phase_start marks phase as "in_progress" in state.json
    - Test: phase_complete marks phase as "completed" in state.json
    - Test: activity-log.jsonl receives entries for init, start, complete events
    - Test: get_resume_phase returns "1a" for fresh state
    - Test: get_resume_phase returns "1b" when only "1a" is completed
    - Test: get_resume_phase returns None when all phases completed
    - Test: GovernanceViolationError is raised when attempting phase "2a" before "1b" completes
    - Test: GateResult defaults to passed=False
  </behavior>
  <action>
    Create `tests/test_pipeline_state.py`:
    - Use `tmp_project_dir` fixture from conftest.py
    - Test init_run: call init_run with app_name="test-app", project_dir=str(tmp_dir), idea="test idea". Assert state.json exists, contains run_id, has all 5 phase IDs as keys with "pending" status.
    - Test phase lifecycle: init_run, then phase_start("1a"), assert state shows "in_progress". Then phase_complete("1a"), assert state shows "completed".
    - Test activity log: After init + start + complete cycle, read activity-log.jsonl. Assert 3+ entries with matching phase_id and event types.
    - Test resume from fresh: init_run, get_resume_phase should return "1a".
    - Test resume after partial: init_run, complete "1a", get_resume_phase should return "1b".
    - Test resume when done: init_run, complete all 5 phases, get_resume_phase should return None.
    - Test PHASE_ORDER constant: assert it equals ["1a", "1b", "2a", "2b", "3"].

    Create `tests/test_governance_monitor.py`:
    - Test GovernanceViolationError can be instantiated with violation_kind string.
    - Test GovernanceMonitor (if the class provides phase ordering checks): attempting to start phase "2a" before "1b" is complete raises GovernanceViolationError with kind "phase_order_violation".
    - Test GateResult: default GateResult has passed=False, creating GateResult(passed=True) has passed=True.

    Note: The governance monitor tests may need adaptation based on the actual API surface of the copied GovernanceMonitor class. Read the source to understand constructor parameters and method signatures before writing tests.
  </action>
  <verify>
    <automated>cd /Users/masa/Development/web-app-factory && uv run pytest tests/test_pipeline_state.py tests/test_governance_monitor.py -v</automated>
  </verify>
  <done>All pipeline state tests pass (init, lifecycle, activity log, resume logic). All governance tests pass (violation detection, gate result). PHASE_ORDER verified as web phases.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_pipeline_state.py tests/test_governance_monitor.py -v` shows all green
- `PHASE_ORDER` in pipeline_state.py equals `["1a", "1b", "2a", "2b", "3"]`
- state.json is created and updated correctly through phase lifecycle
- GovernanceViolationError fires for phase ordering violations
</verification>

<success_criteria>
- 6 production files from ios-app-factory are copied/adapted and importable
- PHASE_ORDER updated to web phases (no iOS phase IDs remain)
- State persistence works: init_run creates state.json, phase lifecycle updates it
- Resume logic works: get_resume_phase returns correct phase after interruption
- Governance enforcement works: out-of-order phases raise GovernanceViolationError
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure/01-02-SUMMARY.md`
</output>
