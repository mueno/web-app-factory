---
phase: 16-mcp-infrastructure-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web_app_factory/_tool_impls.py
  - web_app_factory/mcp_server.py
  - tests/test_tool_impls.py
  - tests/test_tool_annotations.py
autonomous: true
requirements:
  - MCPH-01
  - MCPH-03

must_haves:
  truths:
    - "Both stdio and HTTP servers can invoke identical business logic from _tool_impls.py"
    - "mcp_server.py is a thin wrapper layer with no business logic — only @mcp.tool decorators delegating to impl functions"
    - "All 7 stdio tools have non-None readOnlyHint, destructiveHint, and openWorldHint annotations"
    - "Fixing a bug in _tool_impls.py fixes the behavior in both transports"
  artifacts:
    - path: "web_app_factory/_tool_impls.py"
      provides: "Pure async impl functions for all 7 waf_* tools"
      exports: ["impl_generate_app", "impl_get_status", "impl_approve_gate", "impl_list_runs", "impl_start_dev_server", "impl_stop_dev_server", "impl_check_env"]
    - path: "web_app_factory/mcp_server.py"
      provides: "Thin stdio wrapper with ToolAnnotations on all 7 tools"
      contains: "ToolAnnotations"
    - path: "tests/test_tool_impls.py"
      provides: "Unit tests verifying impl functions are callable and delegate correctly"
    - path: "tests/test_tool_annotations.py"
      provides: "CI test verifying all tools on stdio server have required annotation fields"
  key_links:
    - from: "web_app_factory/mcp_server.py"
      to: "web_app_factory/_tool_impls.py"
      via: "import impl_* functions"
      pattern: "from web_app_factory._tool_impls import impl_"
    - from: "web_app_factory/_tool_impls.py"
      to: "web_app_factory/_pipeline_bridge.py"
      via: "lazy import inside impl functions"
      pattern: "from web_app_factory._pipeline_bridge import"
    - from: "tests/test_tool_annotations.py"
      to: "web_app_factory/mcp_server.py"
      via: "import mcp instance and inspect annotations"
      pattern: "from web_app_factory.mcp_server import mcp"
---

<objective>
Extract shared tool business logic to `_tool_impls.py` and add ToolAnnotations to the stdio MCP server.

Purpose: Establish the shared implementation layer that both stdio and HTTP transports will delegate to — ensuring a fix in one place fixes both. Annotate the stdio server so tool safety hints are visible in manifests.

Output: `_tool_impls.py` with 7 impl functions, slim `mcp_server.py` (~250 lines) with ToolAnnotations, test scaffolds for both.
</objective>

<execution_context>
@/Users/masa/.config/claude/get-shit-done/workflows/execute-plan.md
@/Users/masa/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-mcp-infrastructure-hardening/16-CONTEXT.md
@.planning/phases/16-mcp-infrastructure-hardening/16-RESEARCH.md
@.planning/phases/16-mcp-infrastructure-hardening/16-VALIDATION.md
@web_app_factory/mcp_server.py

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From web_app_factory/mcp_server.py (current — will be refactored):
```python
# 7 tools, all async, using lazy imports inside function bodies:
async def waf_generate_app(idea, mode, deploy_target, company_name, contact_email, resume_run_id) -> str
async def waf_get_status(run_id) -> str
async def waf_approve_gate(run_id, decision, feedback) -> str
async def waf_list_runs() -> str
async def waf_start_dev_server(run_id) -> str
async def waf_stop_dev_server(run_id) -> str
async def waf_check_env(deploy_target, execute_install, tool_to_install) -> str

# Helper functions (private, stay in mcp_server.py):
def _slugify(idea: str) -> str
def _format_disk_status(run_id: str) -> str
def _scan_disk_runs() -> list[dict]
```

From mcp.types (installed, mcp>=1.26.0):
```python
class ToolAnnotations(BaseModel):
    readOnlyHint: bool | None = None
    destructiveHint: bool | None = None
    openWorldHint: bool | None = None
    idempotentHint: bool | None = None
    title: str | None = None
```

From fastmcp (installed, >=3.1.0):
```python
@mcp.tool(annotations=ToolAnnotations(...))
async def tool_name(...) -> str: ...
```

Safety classifications (from RESEARCH.md):
| Tool               | readOnly | destructive | openWorld |
|--------------------|----------|-------------|-----------|
| waf_generate_app   | False    | False       | True      |
| waf_get_status     | True     | False       | False     |
| waf_approve_gate   | False    | False       | False     |
| waf_list_runs      | True     | False       | False     |
| waf_start_dev_server | False  | False       | False     |
| waf_stop_dev_server  | False  | True        | False     |
| waf_check_env      | False    | False       | False     |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract shared impl functions and add ToolAnnotations to stdio server</name>
  <files>web_app_factory/_tool_impls.py, web_app_factory/mcp_server.py, tests/test_tool_impls.py, tests/test_tool_annotations.py</files>
  <behavior>
    - test_tool_impls.py: `_tool_impls` module is importable
    - test_tool_impls.py: All 7 impl functions (impl_generate_app, impl_get_status, impl_approve_gate, impl_list_runs, impl_start_dev_server, impl_stop_dev_server, impl_check_env) exist and are async callables
    - test_tool_impls.py: `_tool_impls.py` has NO module-level singletons (no _STORE, _EXECUTOR, _REGISTRY at module scope)
    - test_tool_annotations.py: Every tool on the stdio `mcp` instance has `annotations is not None`
    - test_tool_annotations.py: Every tool on the stdio `mcp` instance has `readOnlyHint is not None`, `destructiveHint is not None`, `openWorldHint is not None`
    - test_tool_annotations.py: `waf_get_status` has `readOnlyHint=True`; `waf_stop_dev_server` has `destructiveHint=True`; `waf_generate_app` has `openWorldHint=True`
    - Existing tests pass: `uv run pytest tests/test_mcp_server_tool_names.py tests/test_mcp_entry_point.py tests/test_mcp_tools.py -x -q`
  </behavior>
  <action>
    **Step 1: Write tests first (RED phase).**

    Create `tests/test_tool_impls.py`:
    - Test `_tool_impls` is importable
    - Test all 7 `impl_*` functions exist as async callables (use `inspect.iscoroutinefunction`)
    - Test no module-level singletons exist in `_tool_impls` (grep for `_STORE`, `_EXECUTOR`, `_REGISTRY` at module scope)

    Create `tests/test_tool_annotations.py`:
    - Import `mcp` from `web_app_factory.mcp_server`
    - Use `asyncio.run(mcp.list_tools())` to get tool list (same pattern as existing `test_mcp_server_tool_names.py`)
    - For each tool, assert `t.annotations is not None`
    - For each tool, assert `t.annotations.readOnlyHint is not None`, `destructiveHint is not None`, `openWorldHint is not None`
    - Spot-check specific values: `waf_get_status` readOnlyHint=True, `waf_stop_dev_server` destructiveHint=True, `waf_generate_app` openWorldHint=True

    Run tests — they MUST fail (RED).

    **Step 2: Create `_tool_impls.py` (GREEN phase).**

    Create `web_app_factory/_tool_impls.py` with module docstring explaining its role as shared business logic.

    Extract the body of each of the 7 tool functions from `mcp_server.py` into corresponding `impl_*` functions:
    - `impl_generate_app(idea, mode, deploy_target, company_name, contact_email, resume_run_id) -> str`
    - `impl_get_status(run_id) -> str`
    - `impl_approve_gate(run_id, decision, feedback) -> str`
    - `impl_list_runs() -> str`
    - `impl_start_dev_server(run_id) -> str`
    - `impl_stop_dev_server(run_id) -> str`
    - `impl_check_env(deploy_target, execute_install, tool_to_install) -> str`

    CRITICAL rules for `_tool_impls.py`:
    - Keep ALL lazy imports inside the function bodies (preserve existing pattern)
    - NO module-level singletons — all singletons stay in their existing homes (`_pipeline_bridge._EXECUTOR`, `_progress_store._STORE`)
    - Move the helper functions `_slugify`, `_format_disk_status`, `_scan_disk_runs` into `_tool_impls.py` since they are business logic, not transport-specific
    - Use `from __future__ import annotations` at top
    - `_PROJECT_ROOT` can be computed once at module level (it's a path constant, not a singleton)

    **Step 3: Refactor `mcp_server.py` to thin wrappers with ToolAnnotations.**

    Refactor `mcp_server.py`:
    - Keep the `FastMCP(...)` instance creation, `sys.path` setup, and `main()` function
    - Add `from mcp.types import ToolAnnotations` at top
    - Replace each tool body with a one-line delegation to the corresponding `impl_*` function
    - Add `annotations=ToolAnnotations(...)` to each `@mcp.tool()` decorator using the safety classifications from the interfaces block above
    - Remove `_slugify`, `_format_disk_status`, `_scan_disk_runs` from this file (they moved to `_tool_impls.py`)

    After refactor, `mcp_server.py` should be ~200-250 lines (down from 406). Verify with `wc -l`.

    **Step 4: Run all tests (GREEN).**

    Run new tests: `uv run pytest tests/test_tool_impls.py tests/test_tool_annotations.py -x -q`
    Run existing tests: `uv run pytest tests/test_mcp_server_tool_names.py tests/test_mcp_entry_point.py tests/test_mcp_tools.py -x -q`
    All must pass.
  </action>
  <verify>
    <automated>uv run pytest tests/test_tool_impls.py tests/test_tool_annotations.py tests/test_mcp_server_tool_names.py tests/test_mcp_entry_point.py tests/test_mcp_tools.py -x -q</automated>
  </verify>
  <done>
    - `_tool_impls.py` exists with 7 async impl functions and no module-level singletons
    - `mcp_server.py` is under 300 lines with thin wrappers only — no business logic in tool bodies
    - All 7 stdio tools have ToolAnnotations with readOnlyHint, destructiveHint, openWorldHint set to non-None values
    - `waf_get_status` has readOnlyHint=True, `waf_stop_dev_server` has destructiveHint=True, `waf_generate_app` has openWorldHint=True
    - All existing tests still pass (no regressions)
    - New tests pass (test_tool_impls.py, test_tool_annotations.py)
  </done>
</task>

</tasks>

<verification>
- `wc -l web_app_factory/mcp_server.py` < 300 (code-health: down from 406)
- `wc -l web_app_factory/_tool_impls.py` < 400 (code-health compliant)
- `uv run pytest tests/test_tool_impls.py tests/test_tool_annotations.py tests/test_mcp_server_tool_names.py tests/test_mcp_entry_point.py tests/test_mcp_tools.py -x -q` — all green
- No business logic remains in `mcp_server.py` tool function bodies (only `return await impl_*(...)`)
</verification>

<success_criteria>
- MCPH-01: Tool logic extracted to `_tool_impls.py` — `mcp_server.py` delegates to it
- MCPH-03 (partial): Stdio server has all 7 tools annotated with ToolAnnotations
- All existing CI tests pass without regression
- New test scaffolds cover impl extraction and annotation completeness
</success_criteria>

<output>
After completion, create `.planning/phases/16-mcp-infrastructure-hardening/16-01-SUMMARY.md`
</output>
