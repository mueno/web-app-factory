// src/lib/api/errors.ts — generated by web-app-factory
// Provides a standardized error response helper for all API routes.
// Error shape: { error: string, code: string }

/**
 * Returns a JSON Response with the standardized error shape.
 *
 * @param message - Human-readable error description
 * @param code    - Machine-readable error code (e.g. "VALIDATION_ERROR", "NOT_FOUND", "DB_ERROR")
 * @param status  - HTTP status code
 */
export function apiError(message: string, code: string, status: number): Response {
  return Response.json({ error: message, code }, { status });
}
