// src/app/api/health/route.ts — generated by web-app-factory
// Health endpoint that returns service status.
// Pattern: { ok: boolean, service: string, timestamp: string }
// This endpoint is always generated regardless of backend-spec.json contents.

/**
 * GET /api/health
 *
 * Always returns HTTP 200 with service health information.
 * No authentication required — used by load balancers and monitoring.
 */
export async function GET(): Promise<Response> {
  return Response.json({
    ok: true,
    service: "web-app-factory-generated",
    timestamp: new Date().toISOString(),
  });
}
