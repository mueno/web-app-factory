"""web_app_factory — Public API package for Web App Factory MCP server.

This package exposes the public MCP server (waf_ tool namespace) that
end-users interact with via `uvx web-app-factory-mcp`.

The internal pipeline tools live in `tools/` and are not part of this
public surface.
"""

from __future__ import annotations

__version__ = "0.1.0"
